# Drivio

# GitHub sync commit commands
```
git checkout dev
git add .
git commit -m "" -m ""
git push
git checkout staging
git rebase dev
git push origin staging --force
git checkout main
git rebase staging
git push origin main --force
```

```
net start postgresql-x64-18
net stop http
```